const ageCal = function (name, age) {
  return `HI, i am ${name}, and i am ${age} years old`;
};

const info = function (name, skill) {
  return `HI, i am ${name}, and i am ${skill}.`;
};


module.exports = {ageCal, info};